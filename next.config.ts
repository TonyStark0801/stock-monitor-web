import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Removed 'output: export' to support NextAuth API routes
  // If you need static export, consider using Spring Boot OAuth instead
};

export default nextConfig;
